<form action="contact">
	<h4>Bytt kontaktperson</h4>
	<p>Du kan kun bytte mellom personer i innslaget.</p>
	<div class="row">
		<div class="col-xs-12">
			<div class="form-group text-danger">
				<select name="person" class="form-control" required="required">
					<option value="false" disabled="disabled">Velg person</option> 
					{% for person in personer %}
						<option value="{{ person.id }}" {{ person.id == kontaktperson.id ? 'selected' : '' }}>{{ person.fornavn }} {{ person.etternavn }} ({{ person.alder }})</option> 
					{% endfor %}
				</select>
			</div>
		</div>
	</div>
	
	<p>Hvis du vil legge til en ny person som ikke skal delta i innslaget må du:</p>
	<ol class="list col-xs-12">
		<li>Legge til en ny person i innslaget</li>
		<li>Bytte kontaktperson</li>
		<li>Fjerne personen fra innslaget (dette vil ikke fjerne vedkommende som kontaktperson)</li>
	</ol>
	{% if kontaktperson.deltar == false %}
		<p><b>Hvis kontaktperson {{ kontaktperson.fornavn }} {{ kontaktperson.etternavn }} skal være med i innslaget, må personen legges til innslaget igjen på vanlig måte.</b></p>
	{% endif %}

	<div class="clearfix"></div>
	<button type="submit" class="action btn btn-success" data-action="save">Lagre</button>
	&nbsp; &nbsp; eller 
	<a href="#innslag_{{ innslag_id }}" class="innslagResetAndReloadBody" data-innslag-id="{{ innslag_id }}">avbryt</a>
	
	<div class="clearfix"></div>
</form>